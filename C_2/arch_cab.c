#include <stdio.h>
#include <string.h>
//#include < >

/*
para depurar el archivo donde está la función 
principal, se pueden definir las funciones y los tipos de variables (estructuras, enum , union) en un archivo .c 
y los prototipos de funciones y los tipos de variables en un .h (archivo cabecera)

en el archivo de main importamos el .h y también lo importamos en el archivo auxiliar .c
*/  


/*
cuando se quieren correr un código main que está distribuido se pone en el compilador todos los .c involucrados. Los .h no se ponen en la compilación. 

es recomendable realizarlo por partes
gcc -c main.c
gcc -c 
*/

int main(){

    return 0; 
}