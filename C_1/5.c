#include <stdio.h>
/*
este código lo que hace es cambiar de 
un tipo de dato a otro
*/
int main(){

    int a=80; 
    float b=10;
    int suma=a+b; 
    printf("%d \n", suma); 
    printf(" entero %i, flotante %f, double %lf, char %c \n", a, (float)a, (double)a, (char)a); 
    return 0; 
}

/*
si convertimos un número a char lo que saldrá
será su correspondiente en el código aski
*/